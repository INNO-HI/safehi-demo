version: '3.8'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_BASE_URL: http://localhost:4100/core/dashboard
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: always
